<div class="container box">
  <center>
<h2>Edit <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field form-group">
    <label for="username">본인의 이름</label><br />
    <%= f.text_field :username, autofocus: true %>
  </div>

  <div class="field form-group">
    <label for="senior_number">기수</label><br />
    <%= f.number_field :senior_number,min: 1, max: 100, autofocus: true %>기
  </div>

  <div class="field form-group">
    <label for="tel">휴대전화 번호(-없이 입력)</label><br />
    <%= f.telephone_field :tel, autofocus: true %>
  </div>

  <div class="field form-group">
    <label for="tel">학부생일때 제1전공</label><br />
    <%= f.select :major_id, Major.all.collect {|p| [ p.name, p.id ] } %>
    <%#f.text_field :major, autofocus: true%>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <%= f.label :password %> <i>(수정하지 않으려면 빈칸으로 남겨주세요)</i><br />
    <%= f.password_field :password, autocomplete: "off" %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> 자 이상으로 해주세요</em>
    <% end %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :current_password %> <i>(본래 비밀번호가 있어야 수정이 가능합니다)</i><br />
    <%= f.password_field :current_password, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
<% end %>

<h3>회원 탈퇴</h3>

<p>만족스럽지 않습니까? <%= button_to "내 계정을 삭제하세요", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>

<%= link_to "Back", :back %>
</center>
</div>
